package interfaceExample;

public interface Child extends ParentA,ParentB
{
	void childeducation();
}
//then we create child interface and acquires the properties from Two parents class using "extends" keyword.
//we create another 1 method for child interface